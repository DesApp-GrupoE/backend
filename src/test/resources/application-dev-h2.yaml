spring:
  jpa:
    database: default
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: false

merchant:
  datasource:
    url: jdbc:h2:mem:merchant;DB_CLOSE_ON_EXIT=FALSE
    username: desapp
    password: desapp
    driver-class-name: org.postgresql.Driver
